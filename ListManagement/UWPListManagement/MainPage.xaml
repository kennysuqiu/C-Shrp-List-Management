<Page
    x:Class="UWPListManagement.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPListManagement"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <!-- THIS IS A COMMENT -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="15*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="10*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Save" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Save_Click"></Button>
            <TextBox Grid.Column="1"></TextBox>
            <Button Grid.Column="2" Content="Search" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Button>
        </Grid>
        <Grid Grid.Row="1">
            <ListBox ItemsSource="{Binding Path=Items, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition></ColumnDefinition>
                                <ColumnDefinition Width="10*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0">
                                <CheckBox Visibility="{Binding Path=IsCompleteVisibility}" IsChecked="{Binding Path=IsCompleted}"></CheckBox>
                            </Grid>
                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition></RowDefinition>
                                </Grid.RowDefinitions>
                                <TextBlock FontWeight="ExtraBold" Text="{Binding Path=Name}" Grid.Row="0"></TextBlock>
                                <TextBlock Text="{Binding Path=Description}" Grid.Row="1"></TextBlock>
                                <TextBlock Text="{Binding Path=AttendeesString}" Grid.Row="2"></TextBlock>
                            </Grid>
                            
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Content="Add ToDo" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="AddToDoClick"></Button>
            <Button Content="Edit ToDo" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="EditToDoClick"></Button>
            <Button Content="Add Appointment" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="AddAptClick"></Button>
            <Button Content="Edit Appointment" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="EditAptClick"></Button>
            <Button Content="Delete ToDo" Grid.Column="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="DeleteToDoClick"></Button>
            <Button Content="Delete Appointment" Grid.Column="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="DeleteAptClick"></Button>
        </Grid>
    </Grid>
</Page>
